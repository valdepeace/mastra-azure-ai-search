# Azure AI Search Configuration
# Copy this file to .env and fill in your actual values

# Azure AI Search Service URL (e.g., https://your-service.search.windows.net)
AZURE_AI_SEARCH_ENDPOINT=https://your-service.search.windows.net

# Azure AI Search Admin API Key (get from Azure Portal)
AZURE_AI_SEARCH_CREDENTIAL=your-admin-api-key-here

# Optional: API Version (defaults to latest)
AZURE_AI_SEARCH_API_VERSION=2023-11-01

# Optional: Test Index Name Prefix (for testing only)
AZURE_AI_SEARCH_TEST_INDEX_PREFIX=test-mastra-

# Alternative: Connection String Format (for simplified setup)
# AZURE_AI_SEARCH_CONNECTION_STRING=https://your-service.search.windows.net?api-key=your-key
# Advanced Configuration (for custom proxies, custom headers, etc.)
# These are optional and only needed if you're using clientOptions
# PROXY_URL=https://my-proxy.example.com
# PROXY_TOKEN=your-proxy-jwt-token
# CUSTOM_HEADER_VALUE=my-custom-value
